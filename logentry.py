#Log Entry Class


class LogEntry:

	def __init__(self, index, status, data):
		self.index = index
		self.status = status
		self.data = data 

	def process(self):
	# TODO : write function that process the data (string) to get the status of a given log entry
	# can use self.data to access the string

		#if ... :
			#self.status == 'Error'
		#elif ... :
			#self.status == 'Warn'
		#else:
			#self.status == 'OK'


